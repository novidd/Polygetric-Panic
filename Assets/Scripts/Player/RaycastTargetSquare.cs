using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RaycastTargetSquare : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // The comet that is first hit by a raycast from the players forward direction gets a red square attached to it, with a SFX playing
    }
}
